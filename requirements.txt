ariadne[asgi-starlette]==0.20.0 # Para GraphQL
uvicorn[standard]==0.23.2       # Servidor ASGI
SQLAlchemy[asyncpg]==2.0.22     # ORM para interactuar con PostgreSQL de forma asíncrona
alembic==1.12.0                 # Para migraciones de base de datos (opcional pero recomendado)
python-dotenv==1.0.0           # Para cargar variables de entorno desde .env
openai==0.28.1                  # Cliente oficial de OpenAI
aiofiles==23.2.1                # Para operaciones de archivo asíncronas
requests==2.31.0                # Necesario para la biblioteca OpenAI (o httpx si prefieres async para todo)
Pillow==10.0.1                  # Para manipulación de imágenes (si fuera necesario, OpenAI SDK lo puede requerir)
psycopg2-binary==2.9.7         # Adaptador de Python para PostgreSQL (Alembic lo necesita)
asyncpg==0.28.0
